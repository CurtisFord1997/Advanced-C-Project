@model IEnumerable<DrinkUp.Models.ColdCoffee>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ColdCoffeeType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ColdCoffeeName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Caffiene)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ColdCoffeeType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ColdCoffeeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Caffiene)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ColdCoffeeID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ColdCoffeeID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ColdCoffeeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
